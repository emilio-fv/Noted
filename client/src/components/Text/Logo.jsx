import React from 'react';
import Typography from '@mui/material/Typography';

const Logo = ({ sx }) => {
  return (
    <Typography
      variant='h6'
      sx={sx}
    >
      Note-d
    </Typography>
  )
};

export default Logo;